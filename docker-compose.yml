version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: kamal_db
    restart: always
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: kamal_media
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    container_name: kamal_redis
    restart: always
    ports:
      - '6379:6379'

volumes:
  postgres_data:
